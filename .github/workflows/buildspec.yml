version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "WildRydesTaskDefinition"
        LoadBalancerInfo:
          ContainerName: "WildRydesApp"
          ContainerPort: 80
